import { Box, Container, Typography } from "@mui/material";

const Applications = () => {
  return (
    <Box sx={{padding: '2rem 0'}}>
      <Container>
            <Typography>Moderatorga Ariza berish</Typography>
      </Container>
    </Box>
  );
};

export default Applications;
